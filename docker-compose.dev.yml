services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "8501:8501"
    volumes:
      - ./models:/app/models
      - ./src:/app/src
      - ./app:/app/app
      - ./configs:/app/configs
    environment:
      - WANDB_API_KEY=${WANDB_API_KEY}